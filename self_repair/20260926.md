# Self-repair 2026-09-26 — the host hides words (explainer)

**Class:** Data is composited on top of words the scene machine already drew ("reads '10 00'", "'ISS o...tal speed'", "'Before 201'", "'shutdow'"). Count: 24 explainer verdicts on 11 stories across 3 days (09-24..26). Every one was inside an `unreadable` auto-fail, so it counts double. That puts it ahead of low-contrast labels (26 verdicts, but spread over many composers), truncated kickers (17) and `decorative_mascot` (19). Trending's classes were already fixed in #479.
**Fixed:** `viz_scene.render_scene` now records every word (`InkDraw.drawn`) and every host composite (`scene_host` stamps `HOST_MARK`; `watch_hosts`). Words he landed on are re-inked on top of him, clipped to his silhouette and keylined (`ink_over_host`). He is not moved, so a rider stays on his satellite and a load stays in his hands. Offline, the old code composited him over a word in 21 of 465 machine frames (skyline, funnel, inout, race, road). Every one of those words now reads through him (measured, below).
**Test:** `tests/test_the_host_never_hides_a_word.py` renders the named beats (ISS race, Waymo skyline/funnel/inout/road) with and without a visible host. It fails on the old code ("ISS orbital speed" hidden) and passes on the new.
**Still recurring, not fixed today:**
- 6 of the 24 are on the ILLUSTRATED arm (container ships, online shopping, cicada, colon, space travel); today's fix covers the other 18. That arm places the host in `illustrated.py` / `subject_scenes.py`, not `render_scene`. That is the next run's candidate, using the same ledger.
- Low-contrast grey sublines on scene art, and kickers cut with "…" (ISS, passport, lyme).
