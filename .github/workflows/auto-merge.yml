name: Auto-merge claude PRs

# Fires on every PR from a claude/* branch and auto-merges it. No
# diff inspection, no whitelist — the routine is trusted code-author
# and daily.yml has its own auto-pause-on-2-failures safety net, so
# a bad commit self-pauses within a day instead of needing pre-merge
# review.
on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    if: startsWith(github.head_ref, 'claude/') && github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Squash-merge
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh pr merge "${{ github.event.pull_request.number }}" \
            --repo "${{ github.repository }}" \
            --squash --delete-branch
