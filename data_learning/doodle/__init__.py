"""The doodle kit: hand-drawn cartoon scenes for OpenRangeInteractive.

`scene.validate(spec, era)` is the contract; `scene.Scene(spec, era, seed)`
renders it. See each module's docstring for the rules it keeps.
"""
